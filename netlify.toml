# =============================================================================
# NETLIFY PRODUCTION CONFIGURATION
# Site: wellwash.netlify.app
# Proxies /api/* requests and serves the SPA.
# RULE ORDER IS CRITICAL: More specific rules must come first.
# =============================================================================

[build]
  functions = "netlify/functions"